# Overmind Evolution Archive (Mutation Backlog)

이 문서는 오버마인드 시스템이 완벽하지 않음을 인정하고, **향후 반드시 해결하거나 진전시켜야 할 아키텍처적 결함, TODO, 기능 개선점**을 기록하는 유전자 보관소입니다. (시스템 기술 부채 아카이브)

## 0. 최우선 과제 (Critical Mutations)
- [ ] **에이전트 자체의 병렬(Concurrency) 리서치 능력 확보 방안 마련**: 현재 `browser_subagent`를 띄우는 것은 오버헤드가 너무 큼. `search_web`과 `read_url_content`를 활용하여 속도 중심의 병합 리서치를 수행하는 멀티스레딩/병렬 호출 기법을 뇌에 각인해야 함.

## 1. 기관 진화 (Organ Evolution)
- [ ] **Mouth 진화 (API Crawler)**: 기존 `mouth_v1.py`를 버리고, 파이썬의 `asyncio`나 `aiohttp`를 활용하여 수십 개의 URL을 동시에 비동기로 빨아들이는 `fast_crawler.py` 기관 생성 필요.
- [ ] **Digestive System 고도화**: 빨아들인 대용량 텍스트를 LLM Context Limit 내에서 요약/압축하여 뇌(Agent Context)로 넘겨주는 전처리 파이프라인(Map-Reduce 방식 등) 모듈화 필요.

## 2. 유전자/메모리 고도화 (DNA/Memory Management)
- [ ] **메모리 인덱싱(RAG) 도입 검토**: `.agents/memory/`에 쌓이는 마크다운 파일이 수백 개가 넘어갈 경우 뇌가 한 번에 다 읽지 못함. 벡터 DB(Chroma 등)나 단순 키워드 매칭 스크립트를 도입하여 필요한 메모리만 컨텍스트에 Attach 하는 로직 필요.

---
*이 문서는 회고(`회고_및_진화.md`)를 거칠 때마다 지속적으로 추가, 체크(완료) 됩니다.*
